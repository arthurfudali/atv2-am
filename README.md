# atv2-am

## Como rodar o projeto:

Com o UV instalado no seu sistema, rode `uv sync` para baixar as dependências necessárias, navegue até o notebook `at2.apynb` e execute o fluxo de treinamento do projeto
